import Link from 'next/link'; import { TEMPLATES } from '@/lib/mvpClientStore';
export default async function TemplateDetailPage({ params }: { params: Promise<{ id:string }>}){ const {id}=await params; const t=TEMPLATES.find(x=>x.id===id)||TEMPLATES[0];
 return <main className="wide-page"><Link className="back-link" href="/templates">← 返回模板庫</Link><section className="card detail-hero"><div className="template-visual big">{t.emoji}</div><div><h1>{t.name}</h1><p>{t.description}</p><div className="spec-list"><div>建議張數 <b>{t.recommended_count} 張</b></div><div>消耗點數 <b>{t.credit_cost} 點</b></div><div>LINE <b>{t.supports_line?'支援':'不支援'}</b></div><div>動態 <b>{t.supports_animated?'支援':'本輪靜態輸出'}</b></div></div><Link className="btn-primary" href={`/create?template=${t.id}`}>套用此模板</Link></div></section></main> }
